# Logs
*.log
*.tmp
Temp/

# Logs
*.log
*.tmp
Temp/

# Large backups
*.rar
OpenJDK25U-jdk_x64_windows_hotspot_25.0.1_8.msi
TEMP/

config/TaurikAdmins.cs
config/Admin Attempts Log.txt
config/Connections Log.txt
config/Raw Log.txt

# Remote admin console secret (telnet rcon password)
config/RemoteConsole.local.cs

# Per-host deploy settings (dev vs live). Each machine keeps its own so it can
# never travel in a deploy and clobber the live server's. See LIVE_MIGRATION_PLAN.md
config/DeployProfile.cs

# Built deploy packages (artifacts - rebuild from the tag)
_deploy_*/

# Player PII: names + home IPs, written at runtime by Admin.cs.
# Its twin Raw Log.txt was already ignored; this one was missed.
config/Exploit Log.txt
